syntax = "proto3";

option go_package = "./protogen";

message Request {
	bytes Payload = 1;
}

message Response {
	bytes Payload = 1;
}

service TunnelServer {
	rpc Call(stream Request) returns (stream Response);
}